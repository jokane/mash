[[[save test.tex; !exec pdflatex -file-line-error -halt-on-error test > /dev/null; !exec bibtex test; !exec pdflatex test > /dev/null; !exec pdflatex test > test.out; !keep test.pdf
  \documentclass[11pt]{article}
  \usepackage{graphicx}
  
  \begin{document}

    [[[save bib-$1
      @book{bcd,
        author = {abc and def},
        title = {Title of a Book},
        publisher = {xyz},
        year = 2016
      }
    ]]]
    
    Here's a graph~\cite{bcd}:
    \begin{center}
    [[[save $1.dot; !exec sleep 2; !exec dot -Tpdf $1.dot -o $1.pdf; print \includegraphics{$1.pdf}
      graph {
        rankdir=LR
        d -- e
        d -- f
        d -- g
        e -- h
      }
    ]]]
    \end{center}

    Here's another graph.
    \begin{center}
    [[[save g2.dot; !exec sleep 2; !exec dot -Tpdf g2.dot -o g2.pdf; print \includegraphics{g2.pdf}
      graph { rankdir=LR; a -- b -- c -- d; b -- d}
    ]]]
    \end{center}

    More details are in some other document~\cite{abc}.
    [[[save bib-$1
      @book{abc,
        author = {abc and def},
        title = {Title of a Another Book},
        publisher = {xyz},
        year = 2017
      }
    ]]]

    [[[read bib-*; save test.bib]]]

    \bibliographystyle{plain}
    \bibliography{test}

  \end{document}

]]]

